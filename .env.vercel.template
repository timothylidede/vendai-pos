# VendAI POS - Environment Variables Template
# Copy this to .env.local and fill in your actual values

# Firebase Configuration (Required for both web and desktop)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Google OAuth (Desktop version - different from web)
GOOGLE_OAUTH_CLIENT_ID=your_desktop_oauth_client_id

# Google OAuth Web (For Vercel deployment)
NEXT_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_oauth_client_id

# GitHub Integration (Optional - for better API rate limits)
GITHUB_TOKEN=your_github_personal_access_token

# OpenAI API (For AI features)
OPENAI_API_KEY=your_openai_api_key

# Deployment Environment
NEXT_PUBLIC_APP_ENV=production
VERCEL_ENV=production

# Google Maps (Optional - for location features)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Important Notes:
# 1. For Vercel deployment, make sure to add these in your Vercel dashboard
# 2. The Google OAuth client for web and desktop are different - you need both
# 3. Web OAuth client should include app.vendai.digital in authorized domains
# 4. Desktop OAuth client should be configured as "Desktop application" type